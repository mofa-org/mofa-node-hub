# STRICT TEMPLATE - NO DEVIATIONS
[tool.poetry]
name = "CologneAddressGeoNode"
version = "0.1.0"
description = "Auto-generated agent package"
authors = [
    "Mofa Bot <mofa-bot@moxin.com>",
    "Technical Owner <eng@moxin.com>"
]
packages = [{ include = "cologne_address_geo" }] # ⟨⟨critical⟩⟩ MUST PRESERVE USER INPUT

[tool.poetry.dependencies]
python = ">=3.10"
pyarrow = ">= 5.0.0"
requests = "*" # ⟨⟨auto⟩⟩ Detected from HTTP GET request code pattern

[tool.poetry.scripts]
CologneAddressGeoNode = "cologne_address_geo.main:main" # ⟨⟨unmodified⟩⟩ EXACT USER VALUES

[build-system]
requires = ["poetry-core>=1.8.0"]
build-backend = "poetry.core.masonry.api"
