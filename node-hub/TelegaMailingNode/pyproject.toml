# STRICT TEMPLATE - NO DEVIATIONS
[tool.poetry]
name = "TelegaMailingNode"
version = "0.1.0"
description = "Auto-generated agent package"
authors = [
    "Mofa Bot <mofa-bot@moxin.com>",
    "Technical Owner <eng@moxin.com>"
]
packages = [{ include = "telega_mailing_node" }] # ⟨⟨critical⟩⟩ MUST PRESERVE USER INPUT

[tool.poetry.dependencies]
python = ">=3.10"
pyarrow = ">= 5.0.0"
# ⟨⟨auto⟩⟩ Detected from code patterns:
# requests.get → requests = "*"
requests = "*"

[tool.poetry.scripts]
TelegaMailingNode = "telega_mailing_node.main:main" # ⟨⟨unmodified⟩⟩ EXACT USER VALUES

[build-system]
requires = ["poetry-core>=1.8.0"]
build-backend = "poetry.core.masonry.api"
