[tool.poetry]
name = "MockApiNodeIntegrator"
version = "0.1.0"
description = "Auto-generated agent package"
authors = [
    "Mofa Bot <mofa-bot@moxin.com>",
    "Technical Owner <eng@moxin.com>"
]
packages = [{ include = "mock_api_node" }]

[tool.poetry.dependencies]
python = ">=3.10"
pyarrow = ">= 5.0.0"
requests = "*" # Detected: usage of HTTP endpoints implies requests needed

[tool.poetry.scripts]
MockApiNodeIntegrator = "mock_api_node.main:main"

[build-system]
requires = ["poetry-core>=1.8.0"]
build-backend = "poetry.core.masonry.api"
