[tool.poetry]
name = "BinaryJazzGenreStoryNode"
version = "0.1.0"
description = "Auto-generated agent package"
authors = [
    "Mofa Bot <mofa-bot@moxin.com>",
    "Technical Owner <eng@moxin.com>"
]
packages = [{ include = "binaryjazz_genre_story" }]

[tool.poetry.dependencies]
python = ">=3.10"
pyarrow = ">= 5.0.0"
requests = "*" # Detected from HTTP GET code usage

[tool.poetry.scripts]
BinaryJazzGenreStoryNode = "binaryjazz_genre_story.main:main"

[build-system]
requires = ["poetry-core>=1.8.0"]
build-backend = "poetry.core.masonry.api"
