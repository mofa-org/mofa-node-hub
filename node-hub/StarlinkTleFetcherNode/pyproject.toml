# STRICT TEMPLATE - NO DEVIATIONS
[tool.poetry]
name = "StarlinkTleFetcherNode"
version = "0.1.0"
description = "Auto-generated agent package"
authors = [
    "Mofa Bot <mofa-bot@moxin.com>",
    "Technical Owner <eng@moxin.com>"
]
packages = [{ include = "starlink_tle_fetcher" }] # ⟨⟨critical⟩⟩ MUST PRESERVE USER INPUT

[tool.poetry.dependencies]
python = ">=3.10"
pyarrow = ">= 5.0.0"
requests = "*"

[tool.poetry.scripts]
StarlinkTleFetcherNode = "starlink_tle_fetcher.main:main" # ⟨⟨unmodified⟩⟩ EXACT USER VALUES

[build-system]
requires = ["poetry-core>=1.8.0"]
build-backend = "poetry.core.masonry.api"
