# pollen_forecast_node.yml
# Auto-generated configuration for PollenForecastNode

polling:
  # Latitude and longitude for default location (Berlin example)
  latitude: 52.52
  longitude: 13.41
  # List of pollen types to retrieve
  hourly:
    - birch_pollen
    - grass_pollen
  # Base endpoint (should not change unless API changes)
  endpoint: "https://air-quality-api.open-meteo.com/v1/air-quality"
  # Additional request parameters (if needed)
  timeout: 10  # (seconds) reasonable default
  retries: 2   # Number of retries for failed requests
