[tool.poetry]
name = "RpgCharacterSetNode"
version = "0.1.0"
description = "Auto-generated agent package"
authors = [
    "Mofa Bot <mofa-bot@moxin.com>",
    "Technical Owner <eng@moxin.com>"
]
packages = [{ include = "rpg_character_set" }]

[tool.poetry.dependencies]
python = ">=3.10"
requests = "*"
pyarrow = ">= 5.0.0"

[tool.poetry.scripts]
RpgCharacterSetNode = "rpg_character_set.main:main"

[build-system]
requires = ["poetry-core>=1.8.0"]
build-backend = "poetry.core.masonry.api"
